package com.example.skincareroutineplanner.presentation.screens

import androidx.compose.ui.graphics.Color

// Светло-зелёный (основной акцент)
val LightGreen = Color(0xFFA8E6CF)

// Тёмно-зелёный (для контраста, текста, и иконок)
val DarkGreen = Color(0xFF388E3C)

// Белый (фон, карточки)
val White = Color(0xFFFFFFFF)

// Светло-серый (разделители, вторичный текст)
val LightGray = Color(0xFFE0E0E0)

// Тёмно-серый (для текста)
val DarkGray = Color(0xFF424242)

// Цвет ошибки (например, для пропущенной рутины)
val ErrorRed = Color(0xFFEF5350)

val MyRed = Color(0xFFC02626)